export type UserResponse = {
  id: number
  username: string
  email: string
  createdCards: string[]
  favoriteCards: string[]
  likedCards: string[]
  dislikedCards: string[]
  registeredAt: string
}
