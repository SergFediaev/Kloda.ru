import { Container } from '@/components/container'
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Create card',
}

export default function Create() {
  return <Container>Create card</Container>
}
